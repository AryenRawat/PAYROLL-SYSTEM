CREATE TABLE dept (
	deptno int,
	dname varchar(30),
	loc varchar(30));

CREATE TABLE emp (
    empno NUMBER,
    ename VARCHAR2(255), 
    job char(30),
    mgr int,
    hiredate date,
    salary NUMBER, 
    comm int,
    deptno int);